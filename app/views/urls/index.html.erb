<h1> Hello URLS</h1>

<%= form_for @url do |f| %>
  <div class="field">
    <%= f.label :text, "Text" %>
    <%= f.text_field :text, placeholder: "Enter the URL" %>
  </div>

  <div class="actions">
    <%= f.submit "Save Post" %>
  </div>
<% end %>